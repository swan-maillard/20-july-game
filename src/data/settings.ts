/* ============================================================================
   Build / dev toggles.
   ========================================================================== */

/** Show a "skip" button on every game while building. Set to false to ship. */
export const DEV_SKIP = true
